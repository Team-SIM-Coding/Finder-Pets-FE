"use client";
import * as s from "@/shared/styles/common.css";

import { Button } from "@design-system/react-components-button";

const RegisterButton = () => {
  return <Button className={s.defaultButton}>회원가입</Button>;
};

export default RegisterButton;
